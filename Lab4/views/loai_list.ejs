<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">

<div class="container col-8">
<table class="table table-bordered">
    <tr>
        <th>id</th>
        <th>Tên loại</th>
        <th>Thứ tự</th>
        <th>Ẩn hiện</th>
        <th>Điều khiển</th>
    </tr>
    <% for (let c of list ) {  %>
    <tr>
        <td><%= c.id %> </td>        
        <td><%= c.tenLoai %> </td>
        <td><%= c.thuTu %> </td>
        <td><%= c.anHien==1 ? "Đang hiện" : "Đang ẩn"%> </td>
        <td>
            <a href="/loai/edit/<%= c.id %>">Sửa</a> &nbsp;
            <a href="/loai/delete/<%= c.id %>">Xóa</a>
        </td>
    </tr>
    <% } %>
  </ul>
</div>